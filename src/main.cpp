#include <iostream>

#include "../libs/min_maths.h"

#include "./learning/array.h"

int main(int, char **)
{
    /* std::cout << "The sum between 3 and 4 is " << sum(3, 4) << std::endl;
    std::cout << "The substract between 3 and 4 is " << substract(3, 4) << std::endl;
    std::cout << "The multiplication between 3 and 4 is " << multiplication(3, 4) << std::endl;
    std::cout << "The divition between 3 and 4 is " << divition(3, 4) << std::endl; */

    initArray();

    return 0;
}